import numpy as np

a = np.array([2, 3, 1])
b = np.array([1, 2, 5])
scalar_product = np.dot(a, b)
print(scalar_product)

u = np.array([2, 2, 3])
v = np.array([1, 2, -2])
scalar_product = np.dot(u, v)
print(scalar_product)



